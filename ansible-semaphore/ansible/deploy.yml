- name: Update APT packages across sofia-cluster
  hosts: all
  become: true
  
  tasks:
    - name: Deploy ansible-semaphore
      docker_compose:
        project_src: ../docker
        project_name: my_project
        state: present
        files:
          - docker-compose.yml